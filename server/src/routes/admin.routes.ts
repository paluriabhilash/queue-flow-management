import { Router } from 'express';

const router = Router();

// Admin routes placeholder
router.get('/analytics', (req, res) => {
  res.json({ message: 'Admin analytics route ready' });
});

export default router;
